# Credentials for acceptance tests (`make testacc`) and the live smoke test
# in testing/. Copy this file to .env and fill it in — .env is gitignored.
#
# Use a sandbox org: acceptance tests create and destroy real objects.
MASSDRIVER_API_KEY=
MASSDRIVER_ORGANIZATION_ID=
# Only set when not targeting prod:
# MASSDRIVER_URL=
